/*
var users = [
    {name:"lambda",email:"lamda@example.com",roles:[]},
    {name:"admin",email:"admin@example.com",roles:['admin', 'manage-queries']}
];

_.each(users, function (user) {
    var id;

    id = Accounts.createUser({
        email: user.email,
        password: "klsdvklfmkldfqRSScswcx",
        profile: { name: user.name }
    });

    if (user.roles.length > 0) {
        Roles.addUsersToRoles(id, user.roles);
    }

});
 */

//var idAdmin = "qWaH3xgF3EhYYMaXh";
//Roles.addUsersToRoles(idAdmin, ["admin", "manage-users", "manage-queries"]);

